PORT=3005
CORS_ORIGIN=http://localhost:5000

# All three required, all three currently blank — no chain decision has been made.
# Every /mint, /verify, /ledger route returns 503 until all three are set.
RPC_URL=
CHAIN_ID=
CONTRACT_ADDRESS_CONTENT=
CONTRACT_ADDRESS_CREDITS=

# Only needed once minting is real — the wallet that pays gas via the
# Paymaster/Account Abstraction flow described in docs/PRD.md §6.7 FR-7.2.
PRIVATE_KEY=
