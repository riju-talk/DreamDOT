PORT=3004
CORS_ORIGIN=http://localhost:5000

# Required — create a Meta developer app at developers.facebook.com first.
# Every route in this service returns 503 until both are set (see middleware/configured.js).
META_APP_ID=
META_APP_SECRET=
META_OAUTH_REDIRECT_URI=http://localhost:5000/api/meta/oauth/callback
